# 《牛来：牛市接接乐》开发交付说明

版本：MVP / GitHub Pages 首发版  
目标：使用本目录的美术素材，制作一款可在手机和电脑浏览器直接玩的竖屏接物小游戏。

## 给开发 Agent 的开工指令

请直接实现一个无需后端、无需登录、无需构建服务即可部署到 GitHub Pages 的 Web 游戏。技术栈优先使用原生 HTML、CSS、JavaScript 与 Canvas；若确有必要可使用 Phaser 3，但不要引入 React、数据库或服务端。

游戏必须遵守 A 股颜色习惯：**红色代表上涨，绿色代表下跌**。所有分数、碰撞、倒计时和模式切换都要真实可玩，不要只做静态页面。完成后请在本地启动静态服务器进行手机尺寸与桌面尺寸测试，并提供 GitHub Pages 部署说明。

## 1. 产品定义

游戏名：**《牛来：牛市接接乐》**

玩家控制牛来左右移动，接住上涨股票、避开下跌股票。连续接涨积累“牛气值”，充满后进入 8 秒“牛市来了”狂暴模式。单局 60 秒，结算页根据最终分数与大盘指数生成适合截图传播的称号和文案。

核心体验：

- 30 秒学会，60 秒一局。
- 前半段有接物与躲避压力，后半段容易触发一次爽感强烈的牛市模式。
- 结算结果应有梗、适合截图发群。
- 画面使用粗糙、猎奇的 3D 动画质感，不追求写实金融产品感。

## 2. MVP 范围

必须实现：

- 开始页、游戏页、结算页。
- 60 秒倒计时。
- 牛来横向移动。
- 键盘 `←`、`→`、`A`、`D` 操作。
- 手机按住并横向拖动，牛来跟随手指位置。
- 红色上涨物、绿色下跌物及特殊道具随机掉落。
- 碰撞检测、分数、生命、连击、牛气值。
- 8 秒牛市狂暴模式。
- 大盘指数随表现变化。
- 屏幕震动、闪光、浮动分数等基础反馈。
- 结算称号、最高分和再次挑战。
- `localStorage` 保存最高分。
- 响应式竖屏布局与 GitHub Pages 部署。

首版不要实现：

- 登录、账号、后端、真实排行榜。
- 真实股价接口或真实股票名称。
- 商店、装备、养成、广告、联机。
- 复杂关卡系统。

## 3. 画布与响应式

- 游戏逻辑坐标建议：`360 × 640`（9:16）。
- Canvas 始终按比例缩放，最大宽度建议 `480px`。
- 桌面端居中显示，外侧使用深色或行情屏背景。
- 移动端占满可用视口，处理 `devicePixelRatio`，避免画面模糊。
- 牛来的活动区限制在画布底部约 `y = 540`，只允许横向移动。
- HUD 占据顶部约 80–96 逻辑像素，掉落物不能遮挡关键数值太久。
- 页面禁止拖动时滚动与双指缩放干扰；尊重安全区域 `env(safe-area-inset-*)`。

## 4. 游戏状态

建议状态机：

1. `START`：标题、玩法提示、点击任意位置开始。
2. `PLAYING`：普通掉落、计分、连击和牛气积累。
3. `BULL_MODE`：持续 8 秒；红色背景、无绿色股票、分数乘 3。
4. `GAME_OVER`：倒计时归零或生命耗尽后结算。
5. `PAUSED`：页面失焦或切到后台时暂停时间与掉落更新。

切换到 `BULL_MODE` 时需要：

- 牛气值达到 100%。
- 普通背景切换为红金牛市背景，可做 300ms 淡入或闪白过渡。
- 清除场上所有下跌物和熊。
- 牛来切换为狂奔姿势。
- 掉落速度提高约 25%，红色物品生成频率显著提高。
- 分数倍率变为 `×3`。
- 屏幕显示“牛市来了！”并震动一次。
- 8 秒结束后牛气值归零，恢复普通模式。

## 5. 操作与手感

### 键盘

- 左移：`ArrowLeft` 或 `A`。
- 右移：`ArrowRight` 或 `D`。
- 建议移动速度：每秒 260–320 逻辑像素。
- 同时按左右键时停止移动。

### 触控与鼠标

- `pointerdown` 后记录指针位置。
- `pointermove` 时让牛来向指针 X 坐标平滑跟随，不使用虚拟摇杆。
- 使用插值或最大移动速度，避免角色瞬移。
- 点击开始按钮后即可直接拖动。

## 6. 生命、分数与连击

- 初始生命：3 条“牛命”。
- 初始分数：0。
- 初始大盘指数：3248。
- 初始牛气值：0。
- 连续接到上涨物时连击 `combo + 1`。
- 接到任何下跌物或熊时，连击归零。
- 每次成功接涨增加牛气值，建议基础增加 10；连击每 5 次额外增加 5。
- 牛气值最大为 100。

建议分值与效果：

| 类型 | 视觉规则 | 基础分数 | 生命/牛气影响 | 建议权重 |
| --- | --- | ---: | --- | ---: |
| 普通上涨 | 红色上涨牌 | +100 | 牛气 +10 | 35 |
| 强势上涨 | 红色上涨牌，可绘制 `+5.88%` | +200 | 牛气 +12 | 15 |
| 接近涨停 | 红色上涨牌，可绘制 `+9.99%` | +500 | 牛气 +18 | 8 |
| 涨停 | 红色火焰牌 | +1000 | 牛气 +25 | 4 |
| 普通下跌 | 绿色下跌牌 | 0 | -1 生命 | 20 |
| 大跌 | 绿色下跌牌，可绘制 `-5.88%` | 0 | -2 生命 | 7 |
| 跌停 | 绿色骷髅牌 | 0 | 牛气清零、-1 生命 | 3 |
| 火箭 | 红金火箭 | 0 | 5 秒红股价值 ×2、掉落减速 | 3 |
| 白酒 | 白色酒瓶 | +100 | 恢复 1 生命，上限 3 | 2 |
| 利好 | 金色喇叭 | +100 | 5 秒只生成上涨物 | 2 |
| 熊 | 棕色熊 | 0 | 连击清零，3 秒市场偏绿 | 1 |

权重仅用于普通模式，可根据试玩调整。牛市模式中不得生成下跌物、跌停或熊。

最终得分可使用：

```js
finalGain = baseScore * bullMultiplier * rocketMultiplier;
comboBonus = Math.min(combo, 20) * 5;
score += finalGain + comboBonus;
```

## 7. 大盘指数

大盘指数是传播梗，不需要真实行情接口。

- 初始：3248。
- 接到上涨物：按得分增加 `ceil(baseScore / 20)` 点。
- 接到下跌物：减少 15–50 点，但最低不低于 2800。
- 牛市模式中指数增量乘 3。
- 达到 4000、5178、6124、10000 时弹出里程碑文案。
- 指数允许变得夸张，不需要符合现实金融逻辑。

里程碑文案建议：

- 4000：`突破！`
- 5178：`熟悉的位置！`
- 6124：`见证历史！`
- 10000：`这次不一样！`

## 8. 掉落与难度曲线

- 初始每 650–850ms 生成一个物品。
- 初始下落速度约 140–190 px/s。
- 每经过 15 秒，生成间隔缩短约 8%，下落速度提高约 10%。
- 同屏物品建议不超过 12 个。
- 生成 X 坐标时保留物品半径边距。
- 避免连续 3 个高伤害物品落在无法躲避的相邻位置。
- 物品离开底部后回收对象，建议使用对象池避免频繁创建对象。

## 9. 碰撞与判定

- 牛来使用稍小于图片外轮廓的矩形或圆角矩形碰撞盒。
- 掉落物使用圆形或矩形碰撞盒。
- 每个物品只能触发一次效果，触发后立刻标记为不可碰撞并播放消失动画。
- 不要使用逐像素碰撞。
- 接到上涨物：牛来短暂切换接涨姿势 180–250ms。
- 接到下跌物：牛来短暂切换受伤姿势 300–450ms，并给予约 700ms 无敌时间，避免连续扣命。

## 10. HUD 与反馈

顶部 HUD 包含：

- `上证指数 3248.00 ↑`
- 当前分数。
- 剩余时间。
- 连涨 `xN`。
- 三枚牛命图标。
- 牛气值进度条。

反馈要求：

- 接涨：红色浮字、轻微上弹、短促高音效。
- 接跌：绿色闪屏、受伤动作、短震动、低沉音效。
- 涨停：红金爆闪与更大浮字。
- 跌停：牛气条快速归零动画。
- 牛市模式：背景切换、强震动、红色粒子、K 线冲顶。
- 支持 `prefers-reduced-motion`，该模式下减少震动和大幅闪烁。

注意：所有涨跌百分比、股票名、按钮和 HUD 文本由 Canvas/HTML 实时绘制，不要依赖图片内文字。

## 11. 虚构股票名与弹幕

可随机显示：

- 牛来股份
- 牛市科技
- 回本股份
- 解套集团
- 满仓科技
- 东方牛来
- 牛来重工
- 梭哈科技

牛市模式弹幕池：

- 牛来了！
- 牛真的来了！
- 这次不一样！
- 突破！
- 龙一！
- 全体起立！

## 12. 结算页

结算页至少显示：

- `你把大A接到了 XXXX 点`
- 最终分数。
- 最高连击。
- 触发牛市模式次数。
- 本机最高分。
- 对应称号。
- `再接一把` 按钮。
- `挑战 10000 点` 按钮，可与重开共用逻辑。

称号建议：

| 得分 | 称号 |
| ---: | --- |
| 0–999 | 韭菜苗 |
| 1000–2999 | 老股民 |
| 3000–5999 | 抄底侠 |
| 6000–9999 | 私募大佬 |
| 10000–19999 | 牛市发动机 |
| 20000+ | 牛来本人 |

分享文案模板：

> 我把上证接到了 `{index}` 点，获得称号「`{title}`」。牛来，终将抵达。

首版无需调用系统分享接口，提供复制文案按钮即可；如果 `navigator.share` 可用，可以渐进增强。

## 13. 美术素材清单

所有路径均相对于本交付目录。

| 文件 | 用途 | 尺寸 | 接入说明 |
| --- | --- | --- | --- |
| `assets/character/niulai-actions.png` | 牛来六动作表 | 1536×1024 | 3 列×2 行：待机、左跑、右跑、接涨、受伤、牛市狂奔 |
| `assets/items/market-items.png` | 八种掉落物 | 1402×1122 | 4 列×2 行：上涨、涨停、下跌、跌停、火箭、白酒、利好、熊 |
| `assets/backgrounds/normal-market.png` | 普通模式背景 | 941×1672 | 竖屏；草原、K 线、远景金融城 |
| `assets/backgrounds/bull-market.png` | 牛市模式背景 | 941×1672 | 竖屏；红金上涨 K 线与火箭 |

### 关键颜色规则

- 中国 A 股：**红涨、绿跌**。
- `niulai-actions.png` 左下接涨姿势的箭头已修正为红色。
- `niulai-actions.png` 正下受伤姿势的下跌箭头及碎片已修正为绿色。
- 右下牛市狂奔姿势保持红色上涨效果。

### 素材预处理要求

目前角色动作表的棋盘格是烘焙进图片的视觉背景，并非真正透明 Alpha；物品表也带深色底。开发时必须先完成一次性去背与切片：

1. 将角色表按 3×2 区域切出六张图片，再使用图像去背工具删除浅灰棋盘格。
2. 将物品表按 4×2 区域切出八张图片，再删除深色背景。
3. 检查毛发、牛角、火焰和碎片边缘，避免出现白边或黑边。
4. 导出为带透明 Alpha 的 PNG 或 WebP。
5. 保留原始大图，不要覆盖；处理后文件放入对应子目录。
6. 如果自动网格裁切包含较多空白，可逐张收紧画布，但同类物品的视觉尺寸要保持一致。

推荐导出名称：

```text
assets/character/idle.png
assets/character/run-left.png
assets/character/run-right.png
assets/character/catch-up.png
assets/character/hurt-down.png
assets/character/bull-run.png

assets/items/stock-up.png
assets/items/limit-up.png
assets/items/stock-down.png
assets/items/limit-down.png
assets/items/rocket.png
assets/items/liquor.png
assets/items/good-news.png
assets/items/bear.png
```

## 14. 建议项目结构

```text
niulai-game/
├── index.html
├── style.css
├── game.js
├── README.md
└── assets/
    ├── character/
    ├── items/
    ├── backgrounds/
    └── audio/
```

如果使用多个 JS 文件，可拆分为：

```text
src/config.js
src/assets.js
src/entities.js
src/game.js
src/ui.js
```

但不要为了架构完整度引入复杂打包工具。GitHub Pages 应能直接托管静态文件。

## 15. 建议数据结构

```js
const ITEM_TYPES = {
  STOCK_UP:    { score: 100, bull: 10, damage: 0, color: 'red' },
  STRONG_UP:   { score: 200, bull: 12, damage: 0, color: 'red' },
  NEAR_LIMIT:  { score: 500, bull: 18, damage: 0, color: 'red' },
  LIMIT_UP:    { score: 1000, bull: 25, damage: 0, color: 'red' },
  STOCK_DOWN:  { score: 0, bull: 0, damage: 1, color: 'green' },
  STRONG_DOWN: { score: 0, bull: 0, damage: 2, color: 'green' },
  LIMIT_DOWN:  { score: 0, resetBull: true, damage: 1, color: 'green' },
  ROCKET:      { effect: 'rocket' },
  LIQUOR:      { effect: 'heal' },
  GOOD_NEWS:   { effect: 'goodNews' },
  BEAR:        { effect: 'bear' }
};
```

主循环使用 `requestAnimationFrame`，所有移动、掉落和计时基于 `deltaTime`，不要依赖固定帧率。

## 16. 音频建议

首版可使用自制或可商用的简短音效：

- 接涨：清脆提示音。
- 接跌：低沉撞击音。
- 涨停：短促火焰或爆破音。
- 牛市触发：1–2 秒上升音效，随后进入 8 秒循环氛围。
- 按钮：轻微点击音。

首次用户交互前不要自动播放音频。提供静音按钮，并记住用户选择。

## 17. 验收标准

功能验收：

- 从打开页面到开始游戏不超过两次点击。
- 60 秒完整一局，倒计时不受帧率影响。
- 红色上涨物加分，绿色下跌物扣命；颜色和逻辑绝不颠倒。
- 牛气值达到 100 后稳定进入 8 秒牛市模式。
- 牛市模式中不生成绿色下跌物或熊。
- 生命耗尽或时间归零后只能结算一次。
- 重开后所有临时状态正确清零。
- 本机最高分刷新后写入 `localStorage`。

兼容性验收：

- iPhone Safari、Android Chrome 可触控游玩。
- 桌面 Chrome、Edge、Safari 可使用键盘游玩。
- 360×640、390×844、430×932 视口不溢出。
- 页面切到后台时暂停，返回后不会瞬间掉落一堆物品。
- GitHub Pages 子路径部署时资源 URL 不失效，禁止写死站点根路径 `/assets/...`。

体验验收：

- 角色跟手但不瞬移。
- 受伤后有短暂无敌时间。
- 牛市模式与普通模式在背景、速度、音效和特效上有明显差异。
- 文本在明亮背景上仍清晰可读。
- 一局至少有较高概率触发一次牛市模式。

## 18. GitHub Pages 部署

项目应为纯静态资源。资源路径使用相对路径，例如：

```js
const ASSET_ROOT = './assets/';
```

部署方式：

1. 推送到 GitHub 仓库的 `main` 分支。
2. 打开 `Settings → Pages`。
3. 选择 `Deploy from a branch`。
4. 分支选择 `main`，目录选择 `/ (root)`。
5. 等待生成 `https://<username>.github.io/<repo>/`。

## 19. 法务与发布提示

牛来角色来自电影形象。公开发布或商业化前应确认角色、电影剧照及相关名称的使用授权。若仅用于测试或同人传播，应避免暗示官方合作，并在页面中加入合适的非官方说明。不要使用真实证券名称或诱导性投资承诺，可在页脚加入“娱乐作品，不构成投资建议”。

## 20. 最终交付要求

开发 Agent 完成后应交付：

- 可运行的全部源码。
- 已去背、已切片的游戏素材。
- 本地运行说明。
- GitHub Pages 部署说明。
- 一次移动端与桌面端的实际试玩验证结果。
- 已知问题列表；若无则写明“暂无”。

